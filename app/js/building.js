/**
 * Building constructor
 *  
 * @param {Object} id The id of the building
 * @param {Object} name The name of the building
 */
function Building(id, name, token) {
    this.id = id;
    this.name = name;
    this.token = token;
}
